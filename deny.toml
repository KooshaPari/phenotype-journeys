[advisories]
db-path = "$CARGO_HOME/advisory-db"

[licenses]
allow = [
    "MIT",
    "Apache-2.0",
    "BSD-2-Clause",
    "BSD-3-Clause",
    "ISC",
    "Zlib",
    "MPL-2.0",
    "0BSD",
    "CC0-1.0",
]

[bans]
multiple-versions = "warn"
wildcards = "deny"

[sources]
unknown-registry = "warn"
allow-registry = ["https://github.com/rust-lang/crates.io-index"]

# Note: rustls-webpki RUSTSEC-2026-0098/0099/0104 ignores removed —
# advisories not encountered in current dep tree (cargo-deny warned).
