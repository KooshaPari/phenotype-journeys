name: CodeQL
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

on:
  push:
    branches: [main]
    paths-ignore:
      - ".github/workflows/**"
      - "**/*.md"
  pull_request:
    branches: [main]
    paths-ignore:
      - ".github/workflows/**"
      - "**/*.md"
  schedule:
    - cron: "17 4 * * 1"

jobs:
  analyze:
    name: Analyze (${{ matrix.language }})
    runs-on: ubuntu-24.04
    permissions:
      security-events: write
      contents: read
      actions: read
    strategy:
      fail-fast: false
      matrix:
        language: ["javascript", "actions"]
    steps:
      - uses: actions/checkout11bd71901bbe5b1630ceea73d27597364c9af683
      - uses: github/codeql-action/init@865f5f5c36632f18690a3d569fa0a764f2da0c3e
        with:
          languages: ${{ matrix.language }}
      - uses: github/codeql-action/autobuild@865f5f5c36632f18690a3d569fa0a764f2da0c3e
      - uses: github/codeql-action/analyze@865f5f5c36632f18690a3d569fa0a764f2da0c3e
        with:
          category: "/language:${{ matrix.language }}"
