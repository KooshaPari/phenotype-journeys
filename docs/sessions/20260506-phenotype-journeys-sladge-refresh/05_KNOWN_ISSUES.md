# Known Issues

- Older `sladge-current` worktree is behind current canonical.
- Prior validation exposed OCR fixture failures and pre-existing lint/fmt
  drift outside the README-only badge change.
- Online Cargo validation may be unavailable under sandbox DNS.
