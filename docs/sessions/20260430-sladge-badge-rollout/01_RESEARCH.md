# Research

`phenotype-journeys` is a direct sladge candidate. Its README describes a
journey harness that verifies flows with a Claude-describe and Claude-judge
loop, and supports live Anthropic API verification via `ANTHROPIC_API_KEY`.

Canonical `phenotype-journeys` is dirty and ahead/behind origin. The local
AGENTS contract requires feature work in
`repos/phenotype-journeys-wtrees/<topic>/`, so badge work stayed in an isolated
worktree.
